export class Utils{
    // static transformarFecha2(fecha: string): string {
    //     const fechaTransformada = new Date(fecha);
    //     const año = fechaTransformada.getFullYear();
    //     const mes = fechaTransformada.getMonth() + 1; // Los meses van de 0 a 11
    //     const dia = fechaTransformada.getDate();
    
    //     // Agregar ceros iniciales si es necesario
    //     const formatoAño = año.toString().padStart(4, '0');
    //     const formatoMes = mes.toString().padStart(2, '0');
    //     const formatoDia = dia.toString().padStart(2, '0');
    
    //     return `${formatoDia}/${formatoMes}/${formatoAño}`;
    //   }
}